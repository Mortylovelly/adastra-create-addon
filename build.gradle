plugins {
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
}

version = '1.0.0'
group = 'com.mortylovelly.adastracreate'

base {
    archivesName = 'adastra-create-1.20.1'
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(17)
}

minecraft {
    mappings channel: 'official', version: '1.20.1'
    runs {
        client {
            workingDirectory project.file('run')
            mods {
                adastra_create { source sourceSets.main }
            }
        }
        server {
            workingDirectory project.file('run')
            mods {
                adastra_create { source sourceSets.main }
            }
        }
    }
}

repositories {
    maven { url = 'https://maven.minecraftforge.net' }
    maven { url = 'https://maven.terrarium.earth' }
    maven { url = 'https://maven.createmod.net' }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.20.1-47.2.0'
    compileOnly fg.deobf('earth.terrarium.adastra:ad_astra-forge-1.20.1:1.15.20')
    compileOnly fg.deobf('com.simibubi.create:create-1.20.1:0.5.1.f:slim') { transitive = false }
}

jar {
    manifest {
        attributes([
            'Specification-Title'   : 'adastra_create',
            'Specification-Vendor'  : 'mortylovelly',
            'Specification-Version' : '1',
            'Implementation-Title'  : project.name,
            'Implementation-Version': project.jar.archiveVersion,
            'Implementation-Vendor' : 'mortylovelly',
        ])
    }
}
